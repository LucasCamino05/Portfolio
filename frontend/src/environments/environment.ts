/**
 * Environment de desarrollo (default).
 *
 * Para builds de producción, angular.json hace `fileReplacements` y reemplaza
 * este archivo por `environment.production.ts`.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080',
};
